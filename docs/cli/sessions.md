---
summary: "CLI reference for `openx sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
---

# `openx sessions`

List stored conversation sessions.

```bash
openx sessions
openx sessions --active 120
openx sessions --json
```

