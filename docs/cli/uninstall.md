---
summary: "CLI reference for `openx uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `openx uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openx uninstall
openx uninstall --all --yes
openx uninstall --dry-run
```

